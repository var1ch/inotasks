import { StyleSheet } from 'react-native';
import { sizes } from '../../../../../resources';

export default StyleSheet.create({
  container: {
    flex: 1,
    paddingTop: sizes.Margins.marginDefault,
  },
});
